version: "3.8"

services:
  myapitemplate:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: mindee-fileupload
    restart: always
    env_file:
      - app/dev.env
    volumes:
      - "./app:/app"
    ports:
      - "8001:8001"
