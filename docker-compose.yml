version: "3.8"

services:
  myapitemplate:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: mindee-fileupload-dev
    restart: always
    env_file:
      - app/dev.env
    volumes:
      - "./app:/app"
    ports:
      - "8002:8002"
